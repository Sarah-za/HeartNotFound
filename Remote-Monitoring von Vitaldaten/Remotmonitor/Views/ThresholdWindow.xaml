<Window x:Class="Remotmonitor.Views.ThresholdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Schwellenwerte einstellen"
        Width="780" Height="460"
        Background="#111"
        Foreground="White"
        FontFamily="Consolas"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="12">
        <DataGrid x:Name="ThresholdGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  Background="#111"
                  Foreground="White"
                  BorderBrush="#444"
                  AlternatingRowBackground="#1a1a1a"
                  DockPanel.Dock="Top"
                  Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Parameter" Binding="{Binding Parameter}" IsReadOnly="True" Width="120"/>
                <DataGridTextColumn Header="Warning Min" Binding="{Binding WarningMin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Warning Max" Binding="{Binding WarningMax, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Critical Min" Binding="{Binding CriticalMin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Critical Max" Binding="{Binding CriticalMax, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Content="OK"
                    Width="110"
                    Margin="0,0,8,0"
                    Click="OnOkClick"/>
            <Button Content="Abbrechen"
                    Width="110"
                    Click="OnCancelClick"/>
        </StackPanel>
    </DockPanel>
</Window>
